name: formigas_flutter_lints
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  analyzer: ^5.0.0
  analyzer_plugin: ^0.11.2
  custom_lint_builder: ^0.2.9
  custom_lint: ^0.2.5

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
